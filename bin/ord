#!/usr/bin/env bash

# Convert ASCII character to its decimal value
#
# @param ... - characters to convert
ord()
{
	local S L N
	for S
	do
		L=${#S}

		for (( N = 0; N < L; ++N ))
		do
			printf "%d\n" "'${S:$N:1}"
		done
	done
}

if [ "$BASH_SOURCE" == "$0" ]
then
	ord $@
fi
