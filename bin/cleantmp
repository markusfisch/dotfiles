#!/bin/bash

# Remove temporary/waste files
#
# @param 1 - root path (optional)
clean_temp()
{
	local F R=${1:-.}

	for F in \
		'.DS_Store' \
		'Thumbs.db' \
		'*~'
	do
		find "$R" -name "$F" -exec rm {} \;
	done
}

[ "$BASH_SOURCE" == "$0" ] && clean_temp
