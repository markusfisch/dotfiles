#!/usr/bin/env bash

# Find files whose names contain at least one of the given fragments
#
# @param ... - fragments
find_name()
{
	local NAME
	for NAME
	do
		find . -iname "*${NAME}*"
	done
}

if [ "${BASH_SOURCE[0]}" == "$0" ]
then
	find_name "$@"
fi
