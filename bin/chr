#!/usr/bin/env bash

# Convert decimal value(s) to its ASCII representation
#
# @param ... - values to convert
chr()
{
	local N
	for N in "$@"
	do
		printf \\$(printf '%03o' $N)
	done
	echo
}

if [ "$BASH_SOURCE" == "$0" ]
then
	chr $@
fi
