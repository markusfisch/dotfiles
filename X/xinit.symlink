[[ -f ~/.Xresources ]] && xrdb -merge ~/.Xresources

case ${2:-fluxbox} in
	cwm)
		xsetroot -solid '#111'
		piedock
		exec /usr/bin/cwm
		;;
	fluxbox)
		xsetroot -solid '#111'
		piedock
		qbat &
		exec /usr/bin/fluxbox
		;;
esac
